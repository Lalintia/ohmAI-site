# ohmai — starter static site

A minimal static site for Render. No build step.

## Deploy on Render (Static Site)
- Build Command: *(leave empty)*
- Publish Directory: `.`

Then add your custom domain in **Settings → Custom Domains**.
